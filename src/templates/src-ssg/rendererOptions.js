module.exports = function () {
  const opts = {
    /*
    clientManifest: (clientManifest) => {
      clientManifest.all = clientManifest.all.filter((el) => !exclude.some(rx => rx.test(el)))
      clientManifest.async = clientManifest.async.filter((el) => !exclude.some(rx => rx.test(el)))
      return clientManifest
    },

    shouldRenderAsyncScripts: true
    */
  }

  return opts
}
